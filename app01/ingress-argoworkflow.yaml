---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argoworkflow
  namespace: argo
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /argo
            pathType: Prefix
            backend:
              service:
                name: argo-server
                port:
                  number: 2746